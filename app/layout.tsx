import type { Metadata } from "next";
import DashboardLink from "../components/DashboardLink";
import { Geist, Geist_Mono, Poppins, Modak } from "next/font/google";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuLabel,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu"
import "./globals.css";
import Link from "next/link";



const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Gestion de temps de travail",
  description: "Generated by create next app",
};

const poppins = Poppins({
  subsets: ["latin"],
  weight: ["400", "600", "700"], 
  variable: "--font-poppins",
})

const modak = Modak({
  subsets: ["latin"],
  weight: "400", 
  variable: "--font-modak",
})

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {

  
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} ${poppins.variable} ${modak.variable} antialiased`}
      >
        <header
        className="flex justify-between">
          <div className="p-5 font-[modak] text-3xl text-[#000091]"><a href="/..">Gestion TMP</a></div>
          <div className="w-14 h-14 bg-white rounded-full flex items-center justify-center text-gray-700 text-2xl font-bold">
                <DropdownMenu>
                      <DropdownMenuTrigger>👤</DropdownMenuTrigger>
                      <DropdownMenuContent className="bg-indigo-700 p-2 rounded-lg">
                        <DropdownMenuLabel>Mon profil</DropdownMenuLabel>
                        <DropdownMenuSeparator />
                        <DropdownMenuItem>
                              <Link href="/profil">Page utilisateur</Link>
                            </DropdownMenuItem>
                            <DropdownMenuItem>
                              <Link href="/demandes">Demandes</Link>
                            </DropdownMenuItem>
                            <DropdownMenuItem>
                              <DashboardLink />
                            </DropdownMenuItem>
                        {/* <DropdownMenuItem onClick={handleLogout} className="text-red-500"></DropdownMenuItem> */}
                      </DropdownMenuContent>
              </DropdownMenu>

              </div>
        </header>
        {children}
      </body>
    </html>
  );
}
